const data = ['a', 'b', 'c', 'd', 'd'];

const result = data.reduce((prev, curr) => {
  if (prev[curr]) {
    prev[curr] += 1;
  } else {
    prev[curr] = 1
  }
  return prev
}, {})

console.log(result);
